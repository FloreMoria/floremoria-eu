@{
    ViewBag.Title = "Conferma reset password";
}

<style>
    .great-vibes {
        font-family: "Great Vibes", serif;
        font-weight: 400;
        font-style: normal;
    }

    .navbar .navbar-brand,
    .navbar .nav-link {
        color: #369;
    }

    .navbar .nav-link:hover {
        color: white;
    }
</style>

<section class="grid min-h-[80vh] place-items-center bg-gray-100 px-6 py-24 sm:py-32 lg:px-8">
    <div class="text-center">
        <h1 class="mt-4 text-5xl font-semibold tracking-tight text-balance text-gray-900 sm:text-7xl">Ottimo!</h1>
        <p class="mt-6 text-lg font-medium text-pretty text-gray-500 sm:text-xl/8">La tua password è stata reimpostata.</p>
        <div class="mt-10 flex items-center justify-center gap-x-6">
            <a href="/Auth/Login" class="rounded-md bg-[#38679D] px-3.5 py-2.5 text-sm font-semibold text-white shadow-xs hover:bg-[#38679D]/90 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#38679D]">Clicca qui per accedere</a>
        </div>
    </div>
</section>

